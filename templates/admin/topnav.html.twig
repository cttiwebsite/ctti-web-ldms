{% extends 'registration/base.html.twig' %}
{% block body %}
<!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{path('home')}}">
        <div class="sidebar-brand-icon">
            <img src="{{ asset('images/logo3.png') }}" style="width: 50px; height: 50px;" alt="Logo Image">
        </div>
        <div class="sidebar-brand-text mx-3">Go Back</div>
    </a>
{% endblock %}